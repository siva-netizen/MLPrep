import React from 'react'
import ReactDOM from 'react-dom/client'
import StudyTracker from './ml_ai_study_tracker'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <StudyTracker />
  </React.StrictMode>,
)
